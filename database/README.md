# Database Layer (Supabase / PostgreSQL)

Este directorio contendrá:
- `migrations/`: Scripts SQL para la creación y evolución de las tablas en Supabase.
- `seeds/`: Datos iniciales para pruebas locales y fixtures.
- `client.py`: Cliente de acceso a base de datos para el backend.
